当前正在做什么：已完成 X 推文视频下载工具仓库并推送到 GitHub，供后续接帽子云等平台部署。
上次停在哪个位置：已完成 Node 版 H5 页面、同域解析接口、本地实测和 GitHub Actions 校验。
近期关键决定和原因：最终采用 `public/ + server.js` 的 Node 方案，不走纯静态直连；原因是浏览器跨域更不稳，而云平台拉 GitHub 后直接运行 Node 更省事。
